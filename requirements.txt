# Orchestration
langgraph>=0.2

# LLM access (OpenAI-compatible: OpenAI / DeepSeek / Qwen / relays)
openai>=1.40

# Agents, tools, and chat models (LangChain v1: create_agent + @tool)
langchain>=1.0
langchain-core>=1.0
langchain-openai>=1.0

# Validation & config
pydantic>=2.7,<3
pydantic-settings>=2.3
python-dotenv>=1.0

# Optional tracing (Langfuse, OpenTelemetry-based)
langfuse>=4.0

# Storage: PostgreSQL (run metadata + LangGraph checkpoints), optional SSH tunnel
psycopg[binary]>=3.2
psycopg-pool>=3.2
langgraph-checkpoint-postgres>=2.0
sshtunnel>=0.4
# sshtunnel 0.4 references paramiko.DSSKey, removed in paramiko 4+
paramiko<4

# Web API (FastAPI serves the workflow + single-file frontend)
fastapi>=0.115
uvicorn[standard]>=0.30

# Tests
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27            # FastAPI TestClient / fake-model transport

# Code quality
ruff>=0.5
